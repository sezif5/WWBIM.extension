# Issues

- Cannot run Revit 2022 batch runner in this environment, so Task 4 (hands-on QA) requires user-side verification.
- Suggested user QA: run batch runner -> select `fill_dimensions.py` -> confirm no import-time error and that `ADSK_Размер_Объём` populates for at least one `OST_StructuralColumns` and one `OST_StructuralFraming` element.
